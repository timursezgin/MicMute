<Window x:Class="MicMute.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MicMute Settings" Height="628" Width="450" MinHeight="628" MinWidth="450"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        Background="#1E1E1E" FontFamily="Segoe UI" FontSize="14">

    <Window.Resources>
        <!-- Simple Dark Theme Colors -->
        <SolidColorBrush x:Key="CardBackground" Color="#2D2D30"/>
        <SolidColorBrush x:Key="TextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentColor" Color="#0078D4"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3F3F46"/>
        <SolidColorBrush x:Key="InputBackground" Color="#252526"/>

        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                        Grid.Column="2" 
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{StaticResource InputBackground}"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow"
                                                      Grid.Column="1"     
                                                      Fill="{StaticResource TextColor}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False" 
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            Margin="8,4,24,4"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left" 
                                            TextBlock.Foreground="{StaticResource TextColor}"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True" 
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"                
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource InputBackground}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource BorderColor}"
                                            CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Items -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Simple TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Simple Button Style with Windows 11 Rounded Corners -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter x:Name="ContentPresenter"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Simple CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <!-- Audio Device -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Microphone Device" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextColor}" Margin="0,0,0,8"/>
                    <ComboBox x:Name="micComboBox" SelectionChanged="micComboBox_SelectionChanged"/>
                </StackPanel>
            </Border>

            <!-- Hotkey -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Shortcut" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextColor}" Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="hotkeyTextBox" Grid.Column="0" IsReadOnly="True" 
                                 Text="Click here and press a key combination"
                                 PreviewKeyDown="hotkeyTextBox_PreviewKeyDown"/>
                        <Button x:Name="clearHotkeyButton" Grid.Column="2" Content="Clear" 
                                Click="clearHotkeyButton_Click"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Sound Effects -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Sound Effects" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextColor}" Margin="0,0,0,8"/>

                    <TextBlock Text="Mute Sound:" Foreground="{StaticResource TextColor}" Margin="0,8,0,4"/>
                    <ComboBox x:Name="muteSoundComboBox" SelectionChanged="muteSoundComboBox_SelectionChanged"/>

                    <TextBlock Text="Unmute Sound:" Foreground="{StaticResource TextColor}" Margin="0,8,0,4"/>
                    <ComboBox x:Name="unmuteSoundComboBox" SelectionChanged="unmuteSoundComboBox_SelectionChanged"/>
                </StackPanel>
            </Border>

            <!-- Settings -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Settings" FontSize="16" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextColor}" Margin="0,0,0,8"/>

                    <CheckBox x:Name="startWithWindowsCheckBox" Content="Start with Windows"
                              Checked="startWithWindowsCheckBox_Checked" Unchecked="startWithWindowsCheckBox_Unchecked"/>
                    <CheckBox x:Name="startMinimizedCheckBox" Content="Start minimized to system tray"
                              Checked="startMinimizedCheckBox_Checked" Unchecked="startMinimizedCheckBox_Unchecked"/>
                    <CheckBox x:Name="closeToTrayCheckBox" Content="Close button minimizes to tray"
                              Checked="closeToTrayCheckBox_Checked" Unchecked="closeToTrayCheckBox_Unchecked"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>